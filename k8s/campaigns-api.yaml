apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaigns-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: campaigns-api
  template:
    metadata:
      labels:
        app: campaigns-api
    spec:
      containers:
        - name: campaigns-api
          image: ong-campaigns-api:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: ong-config
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: campaigns-api-service
spec:
  type: NodePort
  selector:
    app: campaigns-api
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30002
